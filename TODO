Refactor common code
    duplicate code in newFOOkeys
    marshaljson quoted string creation

keymeta.keys[keyid] = key
keymeta.primary is uint32 keyid

better error messages?  "bad version 0x4 in header" vs. "bad version in header"

Make sure we have real tests cases
    Add test cases for:
        encodeWeb64String
        decodeWeb64String
        lenPrefixPack
        lenPrefixUnpack

function to split a stream into
   header, iv, ciphertext, signature

type header struct {
   uint8 version
   uint32 keyid
}

Split code better among the files:
    pull pkcs5pad into util
    keytypes into key_aes, key_rsa, ... ?

newFOOJsonFromKey()
   - move the construction out to readers and into keydata
   - important for Imported* readers?

Cache key id so it's only calculated once at key load time?

Session encryption (close to being merged; see the python_sessions branch)
    SessionEncrypter/Decryptor objects
    getSessionMaterial

Certificate import

PKCS#8 private key import/export
      block, _ := pem.Decode(buf)
      priv, _ := x509.ParsePKCS1PrivateKey(block.Bytes)
      (dsa not supported -- must implement)
      ImportedKeyReader

Password-based key encryption
    implement pkcs12

Do we want to export all our enums again?
